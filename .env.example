# OpenAI互換推論サーバのURL
# OpenAI APIを使用する場合: 空にするか https://api.openai.com/v1 を指定
# カスタムサーバを使用する場合: http://192.168.1.100:8000 など
AIPERF_URL=

# OpenAI API Key（必須）
# OpenAI APIを使用する場合は必須。カスタムサーバの場合は任意
OPENAI_API_KEY=

# モデル名
# OpenAI APIの場合: gpt-4-small, gpt-4o-mini, gpt-4o など
# カスタムサーバの場合: 推論サーバが認識するモデル名
MODEL=gpt-4-small

# Tokenizer（任意。トークン数計算に使用）
# 無い場合でもTTFT/Latencyのp95/p99は算出可能
TOKENIZER=

# 並行リクエスト数
CONCURRENCY=10

# リクエスト総数（デフォルト: CONCURRENCY * 3）
REQUEST_COUNT=30

# 入力トークン数の平均値（synthetic mode用）
INPUT_TOKENS_MEAN=100

# 入力トークン数の標準偏差（synthetic mode用）
INPUT_TOKENS_STDDEV=20

# 出力トークン数の平均値（synthetic mode用）
OUTPUT_TOKENS_MEAN=200

# リクエストタイムアウト（秒）
REQUEST_TIMEOUT_SECONDS=300

# 任意プロンプト入力ファイル（trace.jsonl形式）
# 指定した場合、synthetic modeの代わりにこのファイルを使用
INPUT_FILE=

# カスタムデータセットタイプ（INPUT_FILE使用時）
# 例: single_turn
CUSTOM_DATASET_TYPE=single_turn

# 追加パラメータ（推論サーバが対応している場合のみ）
# 例: min_tokens:50,ignore_eos:true
# カンマ区切りで複数指定可能
EXTRA_INPUTS=

# ========================================
# オプション: AIPerfサービス設定（macOS問題回避用）
# ========================================
# 以下の設定はmacOSでのサービス登録タイムアウト問題を回避するための設定です。
# Linux環境でも無害に動作しますが、通常は変更不要です。
# scripts/run_aiperf_profile.sh で自動的にデフォルト値が設定されます。

# サービス登録タイムアウト（秒、デフォルト: 120.0）
# AIPERF_SERVICE_REGISTRATION_TIMEOUT=120.0

# サービス登録試行間隔（秒、デフォルト: 2.0）
# AIPERF_SERVICE_REGISTRATION_INTERVAL=2.0

# サービス登録最大試行回数（デフォルト: 20）
# AIPERF_SERVICE_REGISTRATION_MAX_ATTEMPTS=20

# サービス起動タイムアウト（秒、デフォルト: 60.0）
# AIPERF_SERVICE_START_TIMEOUT=60.0
